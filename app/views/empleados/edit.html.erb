	<% if session[:comunidad_id].to_s == @sector.comunidad_id.to_s %>

		<h1>Editing empleado</h1>

		<%= render 'form' %>

		<%= link_to 'Ver', @empleado %> |
		<%= link_to 'Volver', empleados_path %>
	<%else%>
		<h1>Invalid Url</h1>