<%= form_for(@tipo_unidad) do |f| %>
  <% if @tipo_unidad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_unidad.errors.count, "error") %> prohibited this tipo_unidad from being saved:</h2>

      <ul>
      <% @tipo_unidad.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
