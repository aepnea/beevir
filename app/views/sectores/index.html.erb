<h1>Listing sectores</h1>
<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Comunidad</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @sectores.each do |sector| %>
      <tr>
        <td><%= sector.nombre %></td>
        <td><%= sector.comunidad.nombre %></td>
        <td><%= link_to 'Ver', sector %></td>
        <td><%= link_to 'Editar', edit_sector_path(sector) %></td>
        <td><%= link_to 'Eliminar', sector, method: :delete, data: { confirm: 'Está seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Sector', new_sector_path %>
